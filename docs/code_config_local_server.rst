Configuration Management for Local Server
=========================================

This class implements the configuration API as a local http server.
This allows mimicking the same behavior that would be used when provisioning the real hardware via HTTP.

.. automodule:: firmware.config_local_server
   :members:
   :undoc-members:
   :show-inheritance:
